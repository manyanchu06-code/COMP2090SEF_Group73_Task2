# User Guide - Task 2 Binary Tree

1. Make sure Python 3.6+ is installed
2. Download the file `binary_tree.py`
3. Run in terminal / command prompt:
   python binary_tree.py
4. You will see the four traversal results immediately.

No external libraries required (only standard library).
